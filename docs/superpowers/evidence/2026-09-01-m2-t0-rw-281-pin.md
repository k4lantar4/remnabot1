# M2-T0 — Pin Remnawave 2.8.1 restore runtime

Date: 2026-09-01  
Host: RC (`bot-v4` / `91.107.144.95`)  
Production inspect: `ssh bot` (read-only)  
Task: M2-T0 · weight 5  
Authority: `docs/superpowers/plans/2026-08-28-production-cutover-mvp.md`

## Verdict

| Gate | Result |
|---|---|
| P4 `remnawave/backend:2.8.1` pullable | **PASS** |
| P5 production RW compose readable | **PASS** (images + key names; no secret values recorded) |
| Digest pin in rehearsal compose | **PASS** |
| Boot reports 2.8.1 | **PASS** (`🌊 Remnawave Backend v2.8.1`, `__RW_METADATA_VERSION=2.8.1`) |
| Live volumes untouched | **PASS** (`remnawave-db-data` still on sandbox `postgres:18.4`; `remnabot1_postgres_data` still on `remnawave_bot_db`) |
| `rehearsal_bot` not started | **PASS** |
| RC sandbox `backend:3` not used | **PASS** (sandbox remained healthy on `:3` throughout) |

## Blocker found and fixed

First boot of `rehearsal_rw` crash-looped:

```
Environment Configuration Errors:
METRICS_USER: Required
METRICS_PASS: Required
```

Production `/opt/remnawave/.env` has both keys (class D on production). M1-T3 `.env.rehearsal-rw` omitted them.

**Fix:** generated class-C `METRICS_USER=rehearsal_metrics` and `METRICS_PASS` (fingerprint `c4c0153ab9431b28`) in gitignored `.env.rehearsal-rw`. Not the production `METRICS_PASS`. After recreate, Nest started and `/health` returned `{"status":"ok","info":{"database":{"status":"up"}}}`.

## Production identity (`ssh bot`)

| Item | Value |
|---|---|
| Compose | `/opt/remnawave/docker-compose.yml` (`image: remnawave/backend:2.8.1`, tag-only on prod) |
| Running image | `remnawave/backend:2.8.1` |
| Image ID / RepoDigest | `sha256:361f9bb0b183d4fcefea2f1f7163db490e2aa1ec3b4bdde016a9ab9229ce956b` |
| PG | `postgres:17.6` @ `sha256:00bc86618629af00d2937fdc5a5d63db3ff8450acf52f0636ec813c7f4902929` |
| Subscription page | `remnawave/subscription-page:7.2.6` @ `sha256:da5ee26ec70ecd81e57303993e8bfb74c8e52f2fa74644b84aad53324cde2e8c` |
| Volume on prod | compose key `remnawave-db-data` → volume name `remnawave-db` |
| Not copied | hysteria2 JS patch, caddy cert bind-mounts, geosite `.dat`, `TELEGRAM_BOT_TOKEN`, `CLOUDFLARE_TOKEN`, production JWT / `METRICS_PASS` |

Bot app compose `/opt/bot-remnawave/docker-compose.yml` joins external `remnawave-network`; it does not define the panel image.

## RC pull (exact production digest)

```
docker pull remnawave/backend@sha256:361f9bb0b183d4fcefea2f1f7163db490e2aa1ec3b4bdde016a9ab9229ce956b
```

Local inspect: `Id` = same digest; `__RW_METADATA_VERSION=2.8.1`; backend git `ba51868149362d0b9ac0e23133d0532176ccb5a2`; build `2026-07-13T19:41:24Z`. Tagged locally as `remnawave/backend:2.8.1`.

Sandbox `remnawave/backend:3` remains `sha256:add561a4eff6616a0f01d1165e6d48cb7a710d574b4a4c0cae3fbc0cd4c34023` (different; not used).

Companion restore-track images also pulled by production digest: `postgres:17.6`, `remnawave/subscription-page:7.2.6`.

## Compose pin

`deploy/remnawave/docker-compose.rehearsal.yml`:

- `rehearsal_rw`: `remnawave/backend:2.8.1@sha256:361f9bb0b183d4fcefea2f1f7163db490e2aa1ec3b4bdde016a9ab9229ce956b`
- `rehearsal_rw_db`: `postgres:17.6@sha256:00bc86618629af00d2937fdc5a5d63db3ff8450acf52f0636ec813c7f4902929`
- `rehearsal_sub`: `remnawave/subscription-page:7.2.6@sha256:da5ee26ec70ecd81e57303993e8bfb74c8e52f2fa74644b84aad53324cde2e8c`

`docker compose -p rehearsal -f deploy/remnawave/docker-compose.rehearsal.yml config` grep for `remnawave-db-data|remnabot1_postgres|bot-remnawave` → empty.

## Boot proof (then torn down)

`docker compose -p rehearsal -f deploy/remnawave/docker-compose.rehearsal.yml up -d rehearsal_rw` (db + redis deps only; not `rehearsal_sub`; not `rehearsal_bot`).

| Check | Result |
|---|---|
| Container image | `2.8.1@sha256:361f9bb0…ce956b` |
| Logs | `Remnawave Backend v2.8.1`; `migrate:deploy` / `migrate:seed` on **empty** volume |
| `printenv __RW_METADATA_VERSION` | `2.8.1` |
| `curl http://127.0.0.1:3101/health` | HTTP 200 `status=ok` database up |
| Mounts | `rehearsal_rw_pg17`, `rehearsal_rw_valkey_socket` only |
| Sandbox | `backend:3` healthy; `remnawave-db-data` still attached to `postgres:18.4` |

After proof: `compose down` (no `-v`), `docker volume rm rehearsal_rw_pg17`, recreate empty volume so M2-T2 restore does not land on Prisma-seeded empty schema.

## Env derivation (names / fingerprints only)

`.env.rehearsal-rw` (gitignored) public values unchanged from M1-T3: `PANEL_DOMAIN=rw.rookari.com`, `FRONT_END_DOMAIN=*`, `SUB_PUBLIC_DOMAIN=config.rookari.com/sub`, Telegram notifications off, webhook off.

Added M2-T0: `METRICS_USER=rehearsal_metrics`, `METRICS_PASS` fp `c4c0153ab9431b28` (class C). Absent: production `TELEGRAM_BOT_TOKEN`, `CLOUDFLARE_TOKEN`, production JWT, production `METRICS_PASS`.

## Next

M2-T1: restore bot dump into `rehearsal_bot_pg15` (postgres-only). Do not start `rehearsal_bot`. Do not `alembic upgrade` until M4-T0.
