# M4-T3 — Alembic `0111` remnawave_id + boot extras

Date: 2026-09-01  
Host: RC (`bot-v4`)  
Task: M4-T3 · weight 8 · dependencies: M4-T1, M3-ID  
Commit message (this batch): `feat(M4-T3): alembic 0111 remnawave_id and boot extras`

## Verdict: PASS

Decision: **one revision `0111`** (`down_revision='0104'`). No `0112`. Identity columns plus M4-T1 mapped extras in the same inspector-guarded upgrade.

| Check | Result |
|---|---|
| `alembic heads` | **`0111`** only |
| `down_revision` | **`0104`** (grafted remnabot traffic clamp) |
| TDD sqlite upgrade/downgrade | remnawave_id + extras; no deferred **tables**; c2c/wholesale kept |
| Clone round-trip | `0103→0104→0111→0104→0111` **PASS** |
| G1 restore (`127.0.0.1:6061`) | **`0111` (head)** |
| Sandbox `remnawave_bot_db` | still **`0110`**; bot **not** rebuilt |
| `rehearsal_bot` app | **absent** |

## DDL (0111)

- `users.remnawave_id` nullable BIGINT + full unique `ix_users_remnawave_id`
- `users.referral_days_subscription_id`, `users.referral_reward_preference`
- `subscriptions.remnawave_id` nullable BIGINT + partial unique `uq_subscriptions_remnawave_id WHERE remnawave_id IS NOT NULL`
- non-unique `ix_subscriptions_remnawave_short_uuid`
- `subscriptions.grace_candidate_at/reason/suppressed_until` + `ix_subscriptions_grace_candidate`
- `ix_subscriptions_grace_expiry_scan` (status/is_trial/end_date present on remnabot schema)
- `payment_method_configs.description` Text nullable
- `tariffs.lava_product_id` String(255) nullable
- `promocodes.traffic_gb` Integer NOT NULL `server_default='0'` (matches 4.2 model)
- `grace_access_sessions`: **not created**; inspector-guard only if already present (absent on G1)

**Not created:** cispay/platega/lava/coupon/legal/grace **tables**.  
**Not dropped:** C2C, `wholesale_discount_bps`, `user_disabled`.

## Tests (TDD)

File: `tests/database/test_0111_remnawave_id.py`

1. **RED:** missing revision / heads still `0104`.
2. **GREEN:** `uv run pytest tests/database/test_0111_remnawave_id.py tests/database/test_migration_chain.py` → **9 passed**.

## G5 clone (then removed)

Dump: `/tmp/rehearsal_bot_pg15_pre_m4t3.dump`  
SHA-256: `e2fc1efcdaaefc3830cf77b90b9ef778dcc3116444622a88a0f040e48691470b`

Temporary volume `rehearsal_bot_pg15_m4t3_roundtrip` / container `rehearsal_bot_db_m4t3` on `127.0.0.1:6062`. Image `postgres:15-alpine@sha256:3d0f7584ed7d04e27fa050d6683a74746608faf21f202be78460d679cc56461f`.

Alembic CLI used explicit `DATABASE_URL` host `127.0.0.1:6062` (never sandbox).

| Step | Result |
|---|---|
| upgrade `0103→0104` | clamp `leaking_rows=43 leaking_gb=710` |
| upgrade `0104→0111` | head `0111`; identity + extras present; 0 forbidden tables |
| downgrade `0111→0104` | extras gone; wholesale / `user_disabled` / `c2c_receipts.approved_amount_kopeks` remain |
| re-upgrade `0104→0111` | `0111 (head)` |

Clone container and volume **removed** after evidence. G1 volume `rehearsal_bot_pg15` was **not** `-v` removed.

## G5 live restore (G1)

Target printed before CLI: `127.0.0.1:6061/remnawave_bot`.

```
Running upgrade 0103 -> 0104  pre-clamp leaking_rows=43 leaking_gb=710  rows_affected=43
Running upgrade 0104 -> 0111
alembic current = 0111 (head)
```

Post-upgrade inspector (all present = 1): identity extras, `wholesale_discount_bps`, `user_disabled`, `c2c_receipts` + `approved_amount_kopeks`. Forbidden tables: **0**. Indexes include full unique `ix_users_remnawave_id` and partial unique `uq_subscriptions_remnawave_id`. Types: `users`/`subscriptions`.`remnawave_id` bigint nullable; `promocodes.traffic_gb` integer NOT NULL.

## Isolation

| Name | After M4-T3 |
|---|---|
| `rehearsal_bot_pg15` `alembic_version` | **`0111`** |
| `rehearsal_bot` app | **absent** |
| sandbox `remnawave_bot` / `remnabot1_postgres_data` | still **`0110`**; `Created=2026-09-01T10:26:23Z` (not rebuilt) |
| `rehearsal_rw` | 3.4.3 `@sha256:4ea85b2fc16bd3e5d367b61afc07ec219133eaa12dd7b5e898adc33c84515422` |
| clone `rehearsal_bot_db_m4t3` | **removed** |
| live Caddy / `panel.rookari.com` | **unchanged** |

## Not done (next batch)

**M4-T4** models match `0111` (no drift). Do not poll `rehearsal_bot`. Do not rebuild sandbox `remnawave_bot`. Do not `alembic upgrade` / `stamp` sandbox `0110`.
