# M4-T4 — Models match Alembic `0111`

Date: 2026-09-01  
Host: RC (`bot-v4`)  
Task: M4-T4 · weight 3 · dependencies: M4-T3  
Commit message (this batch): `test(M4-T4): lock models to 0111 remnawave_id mapping`

## Verdict: MATCH — `app/database/models.py` unchanged

4.2 already maps identity the way `0111` created it. No model edit. No new Alembic revision.

| Object | Model | G1 (`0111`) |
|---|---|---|
| `users.remnawave_id` | `BigInteger` nullable, `unique=True`, `index=True` → unique `ix_users_remnawave_id` | bigint nullable; `CREATE UNIQUE INDEX ix_users_remnawave_id` |
| `subscriptions.remnawave_id` | plain nullable `BigInteger` (not unique on the column) | bigint nullable |
| partial unique | `__table_args__` `uq_subscriptions_remnawave_id` `WHERE remnawave_id IS NOT NULL` | same |
| extras | referral choice, grace markers, `payment_method_configs.description`, `tariffs.lava_product_id`, `promocodes.traffic_gb` NOT NULL default `0` | present |

## Tests (TDD)

File: `tests/database/test_0111_model_match.py`

1. **RED:** temporary `User.remnawave_id` `unique=False`. `test_user_remnawave_id_is_unique_indexed_bigint` **FAILED**.
2. **GREEN:** unique restored. `uv run pytest tests/database/test_0111_model_match.py tests/database/test_0111_remnawave_id.py tests/database/test_migration_chain.py` → **12 passed**.

## Scoped autogenerate vs G1 (`127.0.0.1:6061`)

`alembic.autogenerate.compare_metadata` against `Base.metadata` (read-only; **no revision written**).

| Class | Count | Action |
|---|---|---|
| Ops on **0111-owned** columns/indexes | **0** | this is the T4 “no diff” |
| `create_table` deferred (cispay/platega/lava/coupons/legal/grace/referral_reward_levels/recurrent) | **9** | do **not** apply (M4-T1/T3) |
| Would **drop** `c2c_receipts` + wholesale/`user_disabled`/partner identity columns | **12** remove ops | do **not** apply (M4-T7) |
| Other on 0111 tables | add `uq_subscriptions_user_tariff_active`; drop `ix_users_business_role` | out of T4; do not apply |

**Do not** run `alembic revision --autogenerate` and commit the result. Full metadata ≠ remnabot restore. Empty/CI `0001` `create_all` still includes deferred 4.2 tables until T7.

## Isolation

| Name | After M4-T4 |
|---|---|
| G1 `rehearsal_bot_pg15` | still **`0111`** (not upgraded further) |
| `rehearsal_bot` app | **absent** |
| sandbox `remnawave_bot` | still **`0110`**; `Created=2026-09-01T10:26:23Z` |
| live Caddy / `panel.rookari.com` | **unchanged** |

## Not done (next batch)

**M4-T5** `persist_identity` seam (panel numeric `.id`, not uuid lookup). Do not poll `rehearsal_bot`. Do not rebuild sandbox `remnawave_bot`.
