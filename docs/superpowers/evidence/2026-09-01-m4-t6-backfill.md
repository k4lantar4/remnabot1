# M4-T6 — Stable-key `remnawave_id` backfill (G6)

Date: 2026-09-01  
Host: RC (`bot-v4`)  
Task: M4-T6 · weight 13 · dependencies: M4-T5, M3-ID  
Panel: `remnawave/backend:3.4.3@sha256:4ea85b2fc16bd3e5d367b61afc07ec219133eaa12dd7b5e898adc33c84515422`  
Bot volume: G1 `rehearsal_bot_pg15` (alembic **`0111`**)  
Commit message (this batch): `feat(M4-T6): stable-key remnawave_id backfill (G6)`

## Verdict: PASS (G6)

M3-ID match keys **agree** with remnabot1 `remnawave_identity_backfill`: exact key is `subscriptions.remnawave_short_uuid` = panel `users.short_uuid`. Script **not rewritten**. Telegram/username/email remain fallbacks only; production-lineage dump has **0** uuid-only rows.

Dry-run default wrote **nothing**. `--apply` filled `subscriptions.remnawave_id` from panel `.id`. Coverage is not low. Remaining 3 rows match M3-ID (2 panel-missing + 1 sibling duplicate).

| Check | Result |
|---|---|
| Match order vs M3-ID | **MATCH** — pass 1 `short_uuid` only |
| Dry-run | 3170 resolved / 3173; `with_id` stayed **0** |
| `--apply` | 3170 written; conflicts **0** |
| G6 join | **3170 matches, 0 mismatches** (`subscriptions.remnawave_id` = panel `users.id` via shortUuid) |
| Multi-tariff | `MULTI_TARIFF_ENABLED=true` → `users.remnawave_id` **0 / 7828** (intentional) |
| Grace table | **absent**; inspector-skip; **not created** |
| Idempotent re-apply | `applied=0`; `with_id` stayed **3170**; same 3 unresolved |
| Coverage | 3170/3173 = **99.91%**; matchable 3170/3172 = **99.94%** (M3-ID) — not a NO-GO |

## Unresolved (3 — expected)

| subscription id | reason | notes |
|---|---|---|
| 4553 | `short_uuid_not_found_in_panel` | short `tZs8JsY8oUU49fRS` missing on panel (M3-ID 2 missing) |
| 82454 | `short_uuid_not_found_in_panel` | short `zSv1uK71P2_4vVDS` missing on panel |
| 81779 | `sibling_shares_panel_account` | expired; short `y-eU-AqbG0KqSqZh` duplicated (dupes=2); panel id 8039 kept on the sibling row |

CLI exit 1 means `report.complete=False` (unresolved remain). Conflicts were 0; apply **committed**.

## G6 sample (`sub.id` → `remnawave_id` = panel `users.id`)

| sub_id | remnawave_id | panel_id | match |
|---|---|---|---|
| 2 | 932 | 932 | t |
| 16 | 946 | 946 | t |
| 17 | 947 | 947 | t |
| 24 | 954 | 954 | t |
| 28 | 958 | 958 | t |
| 96 | 1026 | 1026 | t |
| 97 | 1027 | 1027 | t |
| 102 | 1032 | 1032 | t |

## Auth (class C, fingerprints only)

Restored panel `api_tokens` store uuid+name only (JWT minted at create time; production JWTs cannot be recovered). Rehearsal placeholder `REMNAWAVE_API_KEY` fp `921a2ac8644ae058` does not authenticate.

Minted class-C JWT for new row `api_tokens.name=rehearsal-m4t6-backfill` signed with rehearsal `APP_SECRET` (fp `5bfec8c8bf18af41`), payload `{uuid, username: null, role: API}`. New key fp `f22df80777d66d99` written only to gitignored `.env.rehearsal`. Probe `GET /api/users?size=1` on `127.0.0.1:3100` → **200**.

Did **not** copy production API tokens. Did **not** rewrite live `/opt/remnabot1/.env`.

## Code delta

`_grace_table_exists`: skip `_backfill_grace_sessions` when the table is missing (inspect the **session connection**). TDD: `test_missing_grace_table_is_skipped_not_created` RED (`no such table`) then GREEN. Suite `MULTI_TARIFF_ENABLED=false uv run pytest tests/services/test_remnawave_identity_backfill.py tests/custom/test_persist_identity.py` → **49 passed**.

## Runtime

One-shot `docker run --rm --network rehearsal_net remnabot1-bot:latest python -m scripts.backfill_remnawave_ids` with bind-mounted current `app/` + `scripts/`. Command override — **no polling**. Image was not rebuilt as sandbox `remnawave_bot`.

## Isolation

| Name | After M4-T6 |
|---|---|
| G1 `rehearsal_bot_pg15` alembic | **`0111`** |
| `subscriptions.remnawave_id` | **3170 / 3173** |
| `users.remnawave_id` | **0 / 7828** |
| `grace_access_sessions` | **absent** |
| `rehearsal_bot` app | **absent** |
| sandbox `remnawave_bot` | `Created=2026-09-01T10:26:23Z`; DB **`0110`** |
| `rehearsal_rw` | 3.4.3 digest above; `nodes` **0** |
| live Caddy / `panel.rookari.com` | **unchanged** |

## Not done (next batch)

**M4-T7** port MVP custom behavior (C2C, FA, Toman, wholesale/partner). Do not poll `rehearsal_bot`. Do not rebuild sandbox `remnawave_bot`.
