# M6-T2 — Toman dual-scale regression gate

Date: 2026-09-01  
Host: RC (`bot-v4`)  
Task: M6-T2 · weight 3 · batch M6.1 (this task only) · dependencies: M4-T7  
Commit message (this batch): `test(M6-T2): Toman gate`

## Verdict: PASS (named unit gate + mixed-scale fix)

`tests/utils/test_price_display_toman.py` locks production Toman behavior ported in M4-T7:

- `تومان` suffix and fa-IR comma grouping
- Catalog `price_kopeks` ÷ 100 vs stored balance Toman 1:1
- `BALANCE_TOMAN_CUTOFF_UTC` = `2026-06-05T00:00:00Z`

remnabot has **no** `tests/utils/test_price_display_toman.py`. Closest files are remnabot `tests/utils/test_price_display.py` and `tests/utils/test_balance_display.py` (already partly in remnabot1 as `test_price_display.py`).

**G8 / polling / cabinet UI are not this batch.** User smoke: none.

## TDD

RED: `calculate_missing_amount(9_450, 1_000_000)` returned `990550` (catalog minus Toman).  
GREEN: remnabot-lineage helper — `catalog_price_in_toman(price)` then subtract balance → `550`.

File: `app/services/subscription_renewal_service.py`. Miniapp renewal call site already passed `(balance_kopeks, final_total)` as Toman + catalog kopeks.

## Tests

```text
MULTI_TARIFF_ENABLED=false uv run pytest tests/utils/test_price_display_toman.py tests/utils/test_price_display.py -v
```

**33 passed** (8 named-gate + 25 existing `test_price_display.py`).

`uv run python -c "import main"` — **OK**. `grep -r get_admin_texts app/` — **0**.

## Isolation

Live `/opt/remnabot1/.env` was **not** rewritten. No polling. No compose rebuild.

| Name | After M6-T2 |
|---|---|
| `rehearsal_bot` app | **absent** |
| sandbox `remnawave_bot` | id `3a8fbdb915d3…`; **not rebuilt this batch** |
| live Caddy / `panel.rookari.com` | **unchanged** |

History-query cutoff wiring (`created_after=cutoff` in bot/cabinet) is **not** this gate; the setting/property is locked.
