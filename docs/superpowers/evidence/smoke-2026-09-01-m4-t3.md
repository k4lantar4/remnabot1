# Closed smoke — M4-T3 Alembic 0111 (2026-09-01)

User smoke: none.

| # | Layer | Path | Expect | Status |
|---|---|---|---|---|
| 1 | Agent | `alembic heads` | `0111` only; `down_revision='0104'` | **PASS** |
| 2 | Agent | 0111 sqlite upgrade/downgrade | remnawave_id + extras; no grace/cispay/lava/coupon **tables**; c2c/wholesale kept | **PASS** |
| 3 | Agent | G5 on restore clone then G1 | alembic `0111`; C2C/wholesale columns present | **PASS** |
| 4 | Agent | downgrade round-trip on clone | extras dropped; protected columns remain; re-upgrade → `0111` | **PASS** |
| 5 | Agent | sandbox / `rehearsal_bot` | still `0110`; app absent; not rebuilt | **PASS** |
