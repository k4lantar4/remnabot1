# Closed smoke — M4-T4 models match 0111 (2026-09-01)

User smoke: none.

| # | Layer | Path | Expect | Status |
|---|---|---|---|---|
| 1 | Agent | User `remnawave_id` | `BigInteger` nullable, `unique=True`, `index=True` | **PASS** |
| 2 | Agent | Subscription `remnawave_id` | plain nullable BIGINT + partial unique `uq_subscriptions_remnawave_id` | **PASS** |
| 3 | Agent | 0111 extras mapped | referral/grace/`description`/`lava_product_id`/`traffic_gb` | **PASS** |
| 4 | Agent | scoped autogenerate vs G1 | 0 ALTER of 0111 identity/extras; must not DROP C2C/wholesale/`user_disabled` | **PASS** (0 owned ops; 12 protected drops **not applied**) |
| 5 | Agent | sandbox / `rehearsal_bot` | still `0110`; app absent; not rebuilt | **PASS** |
