# Closed smoke — M4-T5 persist_identity (2026-09-01)

User smoke: none.

| # | Layer | Path | Expect | Status |
|---|---|---|---|---|
| 1 | Agent | `persist_identity` User path | writes `user.remnawave_id` from panel `.id` | **PASS** |
| 2 | Agent | `persist_identity` Subscription path | writes `subscription.remnawave_id` from panel `.id` | **PASS** |
| 3 | Agent | UUID / `resolve_remnawave_id(uuid=…)` | rejected; no uuid lookup | **PASS** |
| 4 | Agent | pytest persist_identity | RED then GREEN | **PASS** |
| 5 | Agent | sandbox / `rehearsal_bot` | still `0110`; app absent; not rebuilt | **PASS** |
