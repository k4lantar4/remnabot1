# Closed smoke — M4-T7 custom port (2026-09-01)

User smoke: none (no polling bot; G8 is M6-T4).

| # | Layer | Path | Expect | Status |
|---|---|---|---|---|
| 1 | Agent | `import main` + `PaymentMethod.C2C` + `C2cReceipt.__tablename__` | starts; enum `c2c`; table `c2c_receipts` | **PASS** |
| 2 | Agent | pytest C2C + Toman + wholesale + FA + identity | 152 passed, 3 skipped (documented) | **PASS** |
| 3 | Agent | `grep -r get_admin_texts app/` | 0 | **PASS** |
| 4 | Agent | no new Alembic / no autogenerate / no cabinet merge | grafted `c2c_receipts` + `0093` wholesale column only | **PASS** |
| 5 | Agent | sandbox / `rehearsal_bot` | still `0110`; app absent; not rebuilt; not polled | **PASS** |
