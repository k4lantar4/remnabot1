# Closed smoke — M5-T1 RC cabinet (2026-09-01)

Evidence: `docs/superpowers/evidence/2026-09-01-m5-t1-rc-cabinet.md`  
Cabinet HEAD: `95d49d8b359afd7d587c6bf3fbdcd9b09a537c15`

| # | Layer | Path | Expect | Status |
|---|---|---|---|---|
| 1 | Agent | `docker compose -f docker-compose.yml -f docker-compose.rc.yml config` | `remnawave-network` only; `VITE_API_URL=/api`; `mrj7_bot`; no `staging-host` / `rehearsal_net` | **PASS** |
| 2 | Agent | recreate cabinet; inspect `remnawave_bot` | cabinet on `remnawave-network` only; bot id unchanged; `rehearsal_bot` not started | **PASS** |
| 3 | Agent | `https://panel.rookari.com/` + `/api/health` + `/api/cabinet/auth/me` | 200 HTML; 200 health `4.2.0`; 401 auth | **PASS** |
| 4 | Agent | telegram-widget + fa JS | `bot_username=mrj7_bot`; `تومان` + `ورود با تلگرام` | **PASS** |
| 5 | User | `https://panel.rookari.com` login with `@mrj7_bot`; switch FA | login works; Persian strings; amounts `تومان` | **pending** |
