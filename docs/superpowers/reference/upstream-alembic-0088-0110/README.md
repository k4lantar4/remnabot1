# Archived remnabot1 upstream Alembic `0088–0110`

Reference-only. These 23 files were the remnabot1 **4.2 / upstream** live graph before M4-T0.

They are **not** on the live Alembic graph. Do not copy them back into `migrations/alembic/versions/`. Do not `alembic upgrade` them onto a remnabot-lineage (`0103` / `0104`) database.

Live graph after M4-T0: remnabot `0088–0104` (`0104_traffic_purchase_expiry_clamp.py`). New work is additive `0111+` from remnabot `0104`.
